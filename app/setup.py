from setuptools import setup, find_packages

setup(
    name="FizzBuzz",
    version="0.1",
    author="AJO",
    packages=find_packages(),
    python_requires='>=3.6',
    tests_require=['pytest']
)
